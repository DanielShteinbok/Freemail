const cryptoAddon = require("./build/Release/crypto_addon.node");

let keyStore = cryptoAddon.crypto_box_keypair();
console.log(keyStore);
// establish SMTP connection

// receive header
//function receiveHeader(addr, key, 

// edit contact

// retrieve file

// yeet file up the chain to the MDA
